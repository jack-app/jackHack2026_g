# トロッコまなベンチャー 実装ドキュメント

このドキュメントでは、アプリがどのように動いているかを説明します。
React が初めての人でも読めるように書いています。

---

## ゲームの流れ（全体像）

アプリはつねに「今どの画面にいるか」を1つの変数で管理しています。
この変数を **フェーズ（phase）** と呼びます。

```
┌──────────┐  スタートボタン   ┌──────────┐
│  title   │ ─────────────▶  │ question │
│(タイトル)│                  │ (問題画面)│
└──────────┘                  └────┬─────┘
      ▲                            │ タイマーが0になる
      │                            ▼
      │                 ┌──────────────────┐
      │                 │ correct/incorrect │
      │                 │  (正解/不正解)    │
      │                 └────────┬─────────┘
      │                          │ 2秒後 自動
      │                          ▼
      │                  ┌─────────────┐
      │                  │ explanation │
      │                  │   (解説)    │
      │                  └──────┬──────┘
      │             次の問題へ  │
      │          ┌──────────────┤
      │          ▼              │ 全問終了
      │    ┌──────────┐         ▼
      │    │ question │   ┌──────────┐  タイトルへ
      │    │(次の問題)│   │  result  │ ──────────▶ title
      │    └──────────┘   │ (結果)   │
      │                   └──────────┘
      └───────────────────────────────
```

フェーズが変わると画面が切り替わります。ページ遷移は使っていません。

---

## ファイル構成

```
src/
├── App.tsx                   ★ アプリの司令塔。フェーズを管理する
├── types.ts                  型の定義（Question と Phase）
│
├── data/
│   └── questions.json        ★ クイズの問題データ（ここを編集する）
│
├── hooks/
│   └── useTimer.ts           タイマーのロジック
│
├── utils/
│   └── shuffle.ts            問題をランダムに並べる関数
│
└── components/
    ├── QuestionScreen.tsx    ★ 問題・選択肢・タイマーバーの画面
    ├── FeedbackScreen.tsx    ★ 正解/不正解を表示する画面
    ├── ExplanationScreen.tsx ★ 解説を表示する画面
    └── TimerBar.tsx          タイマーの残り時間を示すバー
```

★ マークのついたファイルが特に重要です。

---

## クイズデータの追加方法（非エンジニア向け）

問題は `src/data/questions.json` に書きます。
以下のルールに従って追記するだけでOKです。

```json
[
  {
    "id": 1,
    "question": "ここに問題文を書く",
    "choices": ["選択肢A", "選択肢B"],
    "correctIndex": 0,
    "explanation": "ここに解説文を書く"
  }
]
```

| 項目 | 説明 |
|---|---|
| `id` | 問題の番号（1から順番につける。重複NG） |
| `question` | 問題文 |
| `choices` | 選択肢を2つ。`["A", "B"]` の順番で書く |
| `correctIndex` | 正解が何番目か。`0` = 左（A）、`1` = 右（B） |
| `explanation` | 解説文 |

**問題を追加するには？**
最後の `}` の後にカンマをつけて、同じ形式で追記すればOKです。

```json
[
  {
    ...既存の問題...
  },
  {
    "id": 6,
    "question": "新しい問題",
    "choices": ["選択肢A", "選択肢B"],
    "correctIndex": 1,
    "explanation": "解説"
  }
]
```

---

## App.tsx ── アプリの司令塔

`App.tsx` はゲーム全体の状態を持ち、各画面コンポーネントに「何をすべきか」を伝えます。

### 管理している情報（state）

| 変数名 | 意味 |
|---|---|
| `phase` | 今どの画面にいるか |
| `shuffledQuestions` | シャッフルされた問題の配列 |
| `currentIndex` | 今何問目を表示しているか（0始まり） |
| `score` | 正解数 |
| `isCorrect` | 直前の問題が正解だったか |

### 画面の切り替え方

フェーズの値に応じて、表示するコンポーネントを切り替えています。

```tsx
if (phase === 'title')       return <タイトル画面 />;
if (phase === 'question')    return <QuestionScreen />;
if (phase === 'correct' ...) return <FeedbackScreen />;
if (phase === 'explanation') return <ExplanationScreen />;
if (phase === 'result')      return <結果画面 />;
```

### ゲーム開始時の処理（handleStart）

```
1. 問題を全部シャッフルする（順番をバラバラにする）
2. 問題番号を 0 にリセットする
3. スコアを 0 にリセットする
4. フェーズを 'question' にする → 問題画面が表示される
```

### 回答を受け取る処理（handleAnswer）

`QuestionScreen` からタイマー終了時に呼ばれます。

```
受け取るもの: index（選んだ選択肢の番号）または null（未選択のまま時間切れ）

1. index が null でなく、かつ正解の番号と一致していれば → 正解
2. それ以外 → 不正解
3. 正解ならスコアを +1 する
4. フェーズを 'correct' または 'incorrect' にする
```

### 次の問題へ進む処理（handleNext）

```
1. currentIndex を +1 する
2. まだ問題が残っているなら → フェーズを 'question' にして次の問題を表示
3. 全問終了なら → フェーズを 'result' にして結果画面を表示
```

---

## QuestionScreen.tsx ── 問題画面

問題文と2つの選択肢ボタン、タイマーバーを表示します。

### 重要なポイント：「選択」と「回答確定」は別タイミング

ボタンをクリックしても **すぐに正誤判定にはなりません**。
タイマーが0になった時点で、そのとき選んでいた選択肢が最終回答になります。

```
ボタンクリック → 選択状態になる（色が変わる）
                  ↓ 何度でも選び直せる
タイマーが0   → 今選んでいるものを最終回答として送る
```

### selectedIndex と selectedIndexRef の使い分け

| 変数 | 役割 |
|---|---|
| `selectedIndex`（state） | 画面の表示に使う（ボタンの色を変えるため） |
| `selectedIndexRef`（ref） | タイマー終了時に最新の選択を読み出すために使う |

**なぜ2つ必要か？**

Reactのstateは画面を更新するために使いますが、タイマーのような「裏で動いている処理」から読み出すと値が古くなるリスクがあります。
refは「常に最新の値を参照できる箱」なので、タイマー終了時の読み出しに適しています。

```
handleSelect(i) が呼ばれたとき（ボタンをクリック）:
  setSelectedIndex(i)          → UIを更新（ボタンの色が変わる）
  selectedIndexRef.current = i → 最新値をrefにも保存

タイマーが0になったとき:
  onAnswer(selectedIndexRef.current) → refから最新の選択番号を取り出して送る
```

### key={currentIndex} について

`App.tsx` では `<QuestionScreen key={currentIndex} ...>` と書いています。
`key` が変わると React は **コンポーネントを完全に作り直します**。

問題が変わるたびに `currentIndex` が増えるので、`QuestionScreen` が作り直され、
選択状態・タイマーが自動的にリセットされます。

---

## useTimer.ts ── タイマーの仕組み

カウントダウンタイマーのロジックをまとめたカスタムフックです。

### 使い方

```ts
const timeLeft = useTimer(10, () => {
  // タイマーが0になったときに呼ばれる処理
});
```

### 内部の動き

```
マウント時（コンポーネントが初めて表示されたとき）:
  → 1秒ごとに timeLeft を -1 するタイマーを起動

timeLeft が 0 になったとき:
  → onExpire（タイムアウト処理）を1回だけ呼ぶ

アンマウント時（コンポーネントが消えたとき）:
  → タイマーを停止してリセット（メモリリーク防止）
```

### hasExpiredRef について

タイムアウト処理が2回呼ばれないようにするためのフラグです。

```
最初: hasExpiredRef.current = false
タイムアウト発火:
  → false なら → 処理を実行 → true に変える
  → true なら  → 何もしない（2回目以降は無視）
```

---

## FeedbackScreen.tsx ── 正解/不正解画面

「正解！」または「不正解…」を表示し、**2秒後に自動で解説画面へ移ります**。

```
マウント時: setTimeout で2秒後に onNext() を予約する
2秒後: onNext() が呼ばれ、フェーズが 'explanation' になる
```

ボタンはなく、ユーザーは何もしなくても自動で次へ進みます。

---

## ExplanationScreen.tsx ── 解説画面

問題の解説文を表示し、「次の問題へ」または「結果を見る」ボタンを表示します。

`isLast`（最後の問題かどうか）を受け取り、ボタンのラベルを切り替えています。

```
isLast = false → 「次の問題へ ▶」
isLast = true  → 「結果を見る ▶」
```

---

## TimerBar.tsx ── タイマーバー

残り時間を横幅で表現するバーです。

```
残り時間が多い（50%以上）  → 緑色
残り時間が半分以下（25%以上）→ オレンジ
残り時間が少ない（25%未満） → 赤
```

バーの横幅の計算式:
```
横幅（%）= timeLeft ÷ maxTime × 100
```

---

## shuffle.ts ── シャッフル関数

問題の配列をランダムな順番に並べ替えます。
**Fisher-Yates** というアルゴリズムを使っています。

```
やっていること:
  配列の末尾から先頭に向かって1つずつ、
  それより前のランダムな位置の要素と入れ替える。

結果: 全ての並び順が等しい確率で出現する（偏りがない）
```

ゲーム開始時に1回だけ実行し、以降はその結果を順番に使います。
これにより「同じ問題が2回出る」ことがありません。

---

## よくある疑問

**Q. 問題を増やしたい**
→ `src/data/questions.json` に追記するだけ。シャッフルは自動でかかります。

**Q. タイマーの秒数を変えたい**
→ `src/components/QuestionScreen.tsx` の `const MAX_TIME = 10;` の数字を変えてください。

**Q. 正解/不正解画面の表示時間を変えたい**
→ `src/components/FeedbackScreen.tsx` の `setTimeout(..., 2000)` の `2000`（ミリ秒）を変えてください。
　 例: `3000` にすると3秒になります。

**Q. 画面のデザインを変えたい**
→ 各コンポーネントの `style={{ ... }}` の中を変更します。
　 背景画像（PNG素材）に置き換える予定の箇所は `/* 後でpng素材に置き換え予定 */` とコメントがついています。
